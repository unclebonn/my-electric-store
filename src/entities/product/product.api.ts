import { url } from "../../shared/utils/constant";

export const PRODUCT = {
    CUSTOMER: {
        GETPRODUCTALL: `${url}/Product/getAll`,
        GETPRODUCTDETAIL: `${url}/Product/getDetailsById`,
        GETPRODUCTBYSEARCH: `${url}/Product/search`,
        GETPRODUCTBYNAME: `${url}/Product/searchbyName`
    }
}