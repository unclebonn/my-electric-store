import { url } from "../../shared/utils/constant";

export const REVIEW = {
    CUSTOMER: {
        GETALLREVIEW: `${url}/Review/getAll`,
        CREATEVIEW: `${url}/Review/create`,
        GETREVIEWBYPRODUCTID: `${url}/Review/getByProductId`,
    }
}